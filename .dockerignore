# Build artifacts + deps (rebuilt inside the image)
node_modules
.next
out

# VCS
.git
.gitignore
.gitattributes

# Local env (env vars are injected by compose / hosting platform)
.env
.env.local
.env.*.local

# Tests + dev tooling
tests
coverage
.vscode
.idea
*.log
npm-debug.log*
.DS_Store

# Docs / meta
README.md
CLAUDE.md
SRS-Tkxel-AI-Unlimited-v1.1.md

# Docker meta (don't recurse the dockerfiles into the build context)
Dockerfile
Dockerfile.dev
docker-compose*.yml
.dockerignore
